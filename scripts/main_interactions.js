// import { gel, gat } from './utils/shortHands';

function inpCovClicked(element) {
  // var cellIndex = gat(element, "index");
  // var cell = gel("cell-" + cellIndex);
  // cFoc(cell);
}

function cFoc(cell) {
  // if (cell.readOnly == false) {
  //   if (cell.value.length > 0) {
  //     var keyboardButton = _el("keyboardButton");
  //     if (!_isCh("keyboardButton")) {
  //       //cell.setSelectionRange(0,1); 
  //     }
  //   }
  //   _sAttEl(cell, "prevValue", cell.value);
  //   var cellIndex = _gAttEl(cell, "cellIndex");
  //   highlightCell(cellIndex);

  //   _sAtt("pageDivision", "selectedCell", cellIndex);

  //   adjustExcludeForCell(cell);
  //   if (_isCh("showEdit")) {
  //     adjustCellCandidates(cell);
  //   }
  // }

}
export {
  inpCovClicked,
}